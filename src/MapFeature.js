/**
 * @class
 * Represents a single feature of the map (can be a node or way)
 * Knows which layers it should appear in
 * 
 */
export default class MapFeature {
  constructor (properties, featureTags) {}
}